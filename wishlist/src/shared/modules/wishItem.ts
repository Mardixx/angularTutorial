export class WishItem {
  constructor(public wishText: string, public isComplete: boolean = false) { //Basic constructor used in Typescript to set 2 values in this case wishText: a string and isComplete: a boolean
  }
}
